// backend/prisma/schema.prisma

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        Int        @id @default(autoincrement())
  email     String     @unique
  username  String
  password  String
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt
  notebooks Notebook[]
}

model Notebook {
  id          Int      @id @default(autoincrement())
  title       String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  owner       User     @relation(fields: [ownerId], references: [id])
  ownerId     Int
  notes       Note[]
}

model Note {
  id        Int     @id @default(autoincrement())
  title     String
  content   String? @default("")
  
  // --- CAMBIO: Añadimos el tipo de nota ---
  // Valores posibles: 'markdown', 'mermaid', 'code'
  type      String  @default("markdown")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  notebook  Notebook @relation(fields: [notebookId], references: [id])
  notebookId Int
}

// Modelos futuros para tags y colaboración (aún no implementados)
// --- CORRECCIÓN AQUÍ ---
// Se han comentado las líneas una por una
// model Tag {
//   id      Int       @id @default(autoincrement())
//   name    String    @unique
//   notes   NoteTag[]
// }

// model NoteTag {
//   note   Note @relation(fields: [noteId], references: [id])
//   noteId Int
//   tag    Tag  @relation(fields: [tagId], references: [id])
//   tagId  Int

//   @@id([noteId, tagId])
// }

// model Collaborator {
//   id         Int      @id @default(autoincrement())
//   user       User     @relation(fields: [userId], references: [id])
//   userId     Int
//   notebook   Notebook @relation(fields: [notebookId], references: [id])
//   notebookId Int
//   permission String // "VIEWER" o "EDITOR"

//   @@unique([userId, notebookId])
// }

